resume = true

params {
    // CSV file: Format: species_name,genome_fasta
    genomes_csv = null
    protein_db  = null
    min_genes   = 300
}

process {
    executor = 'local'
    cpus     = 1
    memory   = 4.GB
    withName: ALIGN_PROTEOME {
        cpus   = 6
        memory = 12.GB
    }
}
